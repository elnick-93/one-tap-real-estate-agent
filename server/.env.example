TWILIO_SID=
TWILIO_TOKEN=
TWILIO_FROM=
SENDGRID_KEY=
FROM_EMAIL=
STRIPE_SECRET=
STRIPE_PRICE_ID=
JWT_SECRET=change-this
ADMIN_PASSWORD=change-this

# Run: npm install && npm start
# For full SaaS: agents register at /register, use token for dashboard, api_key for Zapier /lead
# Deploy to Railway/Render. Site to Vercel.